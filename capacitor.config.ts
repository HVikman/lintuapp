import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lintuapp.app',
  appName: 'lintu',
  webDir: 'backend/lintu',
  bundledWebRuntime: false
};

export default config;
